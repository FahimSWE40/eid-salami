<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <title>Send Eid Salami</title>
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <style>
    body { font-family: sans-serif; text-align: center; background: radial-gradient(circle at top, #fff9e6, #fceabb); padding: 40px 10px; margin: 0; }
    h1 { font-size: 2em; color: #d63384; text-shadow: 0 0 5px #ffb3ec; margin-bottom: 20px; }
    form { display: inline-block; background: #ffffffcc; padding: 20px; border-radius: 15px; box-shadow: 0 0 15px rgba(0,0,0,0.1); max-width: 400px; width: 90%; }
    input, textarea { width: 100%; padding: 10px; font-size: 16px; margin: 10px 0; border: 2px solid #ddd; border-radius: 8px; }
    textarea { resize: none; }
    button { background-color: #28a745; color: white; padding: 12px 25px; font-size: 16px; border: none; border-radius: 8px; cursor: pointer; }
    button:hover { background-color: #218838; }
    .emoji { font-size: 1.5em; margin-bottom: 10px; }
  </style>
</head>
<body>
  <div class="emoji">🧧✨🌙 Send Funny Eid Salami 🌙✨🧧</div>
  <h1>Create Your Eid Salami Link</h1>
  <form action="/send" method="POST">
    <input type="text" name="name" placeholder="Your Name" required />
    <textarea name="message" placeholder="Your Eid message..." rows="3"></textarea>
    <button type="submit">Send Salami</button>
  </form>
</body>
</html>