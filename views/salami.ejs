<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0"/>
  <title>Eid Salami 🎁</title>
  <style>
    body {
      font-family: 'Segoe UI', sans-serif;
      background: linear-gradient(135deg, #fff6e0, #ffe3ec);
      text-align: center;
      margin: 0;
      padding: 2rem 1rem;
    }

    .eid-emoji {
      font-size: 2rem;
      margin-bottom: 1.2rem;
      animation: float 3s ease-in-out infinite;
    }

    .card {
      background: white;
      padding: 2rem;
      border-radius: 20px;
      box-shadow: 0 12px 30px rgba(255, 105, 180, 0.3);
      display: inline-block;
      width: 100%;
      max-width: 420px;
      animation: glow 2.5s ease-in-out infinite;
    }

    h1 {
      color: #d63384;
      font-size: 1.8rem;
      margin-bottom: 1rem;
      text-shadow: 0 0 8px #ffc0e8;
    }

    .note-label {
      font-size: 1.1rem;
      color: #555;
      margin-bottom: 0.5rem;
    }

    img {
      width: 100%;
      height: auto;
      max-width: 300px;
      border-radius: 12px;
      margin: 1rem 0;
      box-shadow: 0 4px 15px rgba(0, 0, 0, 0.1);
    }

    p {
      font-size: 1.05rem;
      color: #444;
      margin: 10px 0;
      line-height: 1.5;
      padding: 0 10px;
    }

    .counter {
      font-size: 0.9rem;
      color: #777;
      margin-top: 1rem;
    }

    audio {
      display: none;
    }

    .make-own-btn {
      display: inline-block;
      margin: 1.5rem auto 0;
      background-color: #28a745;
      color: white;
      padding: 12px 25px;
      border-radius: 10px;
      font-size: 1rem;
      text-decoration: none;
      font-weight: bold;
      box-shadow: 0 4px 10px rgba(0,0,0,0.15);
      transition: background-color 0.3s ease;
    }

    .make-own-btn:hover {
      background-color: #218838;
    }

    footer {
      margin-top: 2rem;
      padding: 10px;
      font-size: 0.9em;
      color: #777;
    }

    footer a {
      color: #d63384;
      text-decoration: none;
      font-weight: bold;
    }

    footer a:hover {
      text-decoration: underline;
    }

    @keyframes float {
      0%, 100% { transform: translateY(0); }
      50% { transform: translateY(-8px); }
    }

    @keyframes glow {
      0%, 100% { box-shadow: 0 0 15px 4px #f797d1; }
      50% { box-shadow: 0 0 25px 8px #ffa3d3; }
    }

    @media (max-width: 480px) {
      body {
        padding: 1.5rem 0.5rem;
      }

      .card {
        padding: 1.5rem 1rem;
        border-radius: 16px;
      }

      h1 {
        font-size: 1.4rem;
      }

      .eid-emoji {
        font-size: 1.6rem;
      }

      p, .note-label {
        font-size: 1rem;
      }

      .make-own-btn {
        font-size: 0.95rem;
        padding: 10px 20px;
      }
    }
  </style>
</head>
<body>

  <div class="eid-emoji">🌙✨🕌 Eid Mubarak! 🕌✨🌙</div>

  <div class="card">
    <h1>💸 Eid Salami from <%= name %> 💸</h1>
    <div class="note-label">Here’s your salami note:</div>
    <img src="<%= note %>" alt="TK Note" />
    <p><%= message %></p>
  </div>

  <!-- 👇 Button to return to home/index -->
  <a href="/" class="make-own-btn">🎁 Make Your Own Link</a>

  <audio autoplay loop>
    <source src="<%= song %>" type="audio/mpeg">
    Your browser does not support the audio tag.
  </audio>

  <footer>
    © Developed with ❤️ by 
    <a href="https://github.com/FahimSWE40" target="_blank">Fahim</a>
  </footer>

</body>
</html>
